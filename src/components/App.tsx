import React from 'react';
import './App.scss';
import Table from './Table';

const App: React.FC = () => {
    return (
        <div className="container">
            <Table />
        </div>
    );
};

export default App;
